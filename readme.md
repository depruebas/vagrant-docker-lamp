It's necessary install vagrant-host-shell  for modify host files

First, execute this command in a terminal

vagrant plugin install vagrant-host-shell

After install, run in the project directory

vagrant up